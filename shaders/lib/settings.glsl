#define UPSCALE_TERRAIN // Whether to upscale terrain (all blocks) or not.
#define UPSCALE_SKY // Whether to upscale sky textures (i.e sun, moon, custom skybox) or not. Has a quite large performance impact.
#define UPSCALE_ENTITIES // Whether to upscale entities or not.
#define UPSCALE_HAND // Whether to upscale your hand or it's items or not.
#define UPSCALE_PARTICLES // Whether to upscale particles or not. Does not affect weather.
#define UPSCALE_BEACONBEAM // Whether to upscale the beacon beam or not.
//#define UPSCALE_SCREEN // Upscales game screen. Hits performance pretty bad. Works "ok" as an anti-aliasing filter.

#define INFO 0 // xBRZUpscale shader by Ashie! [0 1]